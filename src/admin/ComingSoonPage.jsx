// Placeholder for the CRUD sections that come one table at a time in
// later phases — not linked to anything functional yet.
function ComingSoonPage({ title }) {
  return (
    <div>
      <h1 className="font-heading text-2xl text-[color:var(--color-text-primary)]">{title}</h1>
      <p className="mt-2 text-[color:var(--color-text-secondary)]">Coming soon.</p>
    </div>
  )
}

export default ComingSoonPage
